version: '2'
services:
  reactdashboard-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/reactdashboard-mongodb-config --bind_ip 0.0.0.0
  reactdashboard-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  reactdashboard-mongodb-config:
    image: mongo:4.2.7
    container_name: reactdashboard-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
